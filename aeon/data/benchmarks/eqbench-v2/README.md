eq-bench-v2 (https://github.com/EQ-bench/EQ-Bench)

Note that by default this only has the LLM produce scores, not free text. Need to run the benchmark with "--revise" flag if I want to get free text.
